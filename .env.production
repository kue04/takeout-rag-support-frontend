# 生产构建配置。`vite build` 默认 --mode production，会加载本文件，
# 且优先级高于 .env.local —— 这一步很关键：否则本机开发用的 127.0.0.1 会被一起烧进产物。
#
# 本文件不含任何秘密，所以必须进仓库（.gitignore 里有 `!.env.production` 例外）。
#
# ── 为什么这两项必须留空（踩坑 C8）─────────────────────────────────────────
# Vite 会把所有 VITE_* 变量在**构建期**内联成字符串字面量写进 dist/*.js。
# 而 dist 是静态文件，一上线就是人人可 curl 的东西。所以：
#
#   VITE_API_BASE_URL 留空 ⇒ 请求走同源相对路径（/chat/prompt），
#     由反向代理把 / 转发到后端。一旦填了绝对地址（尤其 127.0.0.1:xxxx），
#     每个访客的浏览器都会去请求**他自己那台机器**的端口 —— 服务器上的后端无人访问。
#
#   VITE_DEV_TOKEN 留空 ⇒ 产物里不含任何凭据。
#     代价：这样构建出来的前端没有令牌，所有受保护接口都会 401。
#     这是**故意的** —— 缺凭据就该报错，不能拿一个开发令牌顶上。
#     同一条原则见踩坑 C7（脚本缺 TOKEN 就 exit 2，不退化成「跳过鉴权」）。
#     线上身份改由运行时登录换取；真登录落地后这个变量整体删除。

VITE_API_BASE_URL=
VITE_DEV_TOKEN=
