# 本文件是模板：本地开发复制成 .env.local 再填。
# 生产构建**不看**本文件，看 .env.production（那里两项都必须留空，原因见该文件内注释）。

# 后端地址（本地开发填本机后端端口）。
#   生产构建留空 ⇒ 同源相对路径 /chat/prompt，由反向代理转发到后端。
# ⚠️ VITE_ 前缀的变量会被**内联进 dist/*.js 并随产物公开**，所以这里只放非秘密的值。
VITE_API_BASE_URL=http://127.0.0.1:8001

# 开发期 JWT。身份**只**来自 Authorization: Bearer <JWT>；
# X-User-Role / X-Operator-Id 自后端阶段 1 起已彻底失效（只记日志，不参与判定）。
#
# ⚠️ 这个值同样会被内联进前端产物。只允许出现在 .env.local（本机联调），
#    绝不允许出现在 .env.production —— 那等于把一个 admin 令牌公开发布（踩坑 C8）。
#    线上身份改由运行时登录换取（待实现）。
#
# 生成方式（在后端仓库执行）：
#   ./venv/Scripts/python.exe scripts/mint_dev_token.py --role admin --format token
# 角色可选：agent / supervisor / knowledge_ops / qa / admin
VITE_DEV_TOKEN=
